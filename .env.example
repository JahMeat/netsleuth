# Where the PartyKit room server lives.
#   local:      127.0.0.1:1999   (what `npm run dev:party` serves)
#   deployed:   netsleuth.<your-partykit-username>.partykit.dev
# Set the deployed value in the Vercel project's environment variables.
NEXT_PUBLIC_PARTYKIT_HOST=127.0.0.1:1999
